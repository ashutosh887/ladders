// @ts-nocheck

// if the player wins... show alert
const onWinnerALert = (diceClickCount) => {
  alert(
    `The player had to roll the dice ${diceClickCount} times to finish the Game! 🙌`
  );
};

export default onWinnerALert;
